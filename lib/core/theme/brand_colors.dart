import 'package:flutter/material.dart';

/// Brand identity colors, separate from future financial/status colors.
class BrandColors {
  BrandColors._();

  static const deepNavy = Color(0xFF0B1F33);
  static const cyan = Color(0xFF00C2E6);
  static const coral = Color(0xFFFF7B6E);
  static const offWhite = Color(0xFFF4F7FA);

  static const textOnLight = deepNavy;
  static const textOnDark = offWhite;
  static const contentOnCyan = deepNavy;
  static const contentOnCoral = deepNavy;
}
